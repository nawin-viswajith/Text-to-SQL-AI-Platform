[project]
name = "enterprise-text2sql-backend"
version = "0.1.0"
description = "Enterprise Text-to-SQL backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["app/tests"]
pythonpath = ["."]

